#include "main.h"
#include <string.h>
/**
 * _strlen - reset a value to 98 using pointer
 *
 * @s: pointer to char
 *
 * Description: return length of a string
 *
 * Return: int value
 */

int _strlen(char *s)
{
	return (strlen(*s));
}
