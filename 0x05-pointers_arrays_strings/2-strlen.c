#include "main.h"
#include <string.h>
/**
 * _strlen - gets the length of a string
 *
 * @s: pointer to char
 *
 * Description: return length of a string
 *
 * Return: int value
 */

int _strlen(char *s)
{
	return (strlen(s));
}
